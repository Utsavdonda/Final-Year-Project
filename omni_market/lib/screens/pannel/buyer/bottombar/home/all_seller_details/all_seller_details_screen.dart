import 'package:flutter/material.dart';

class SellersDetailsScreen extends StatelessWidget {
  const SellersDetailsScreen({super.key, required this.index});
  final int index;

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
